name: kafa_member
description: KAFA Member Portal — Demo for philmathieu.com portfolio.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  http: ^1.2.0
  crypto: ^3.0.3
  provider: ^6.1.2
  cupertino_icons: ^1.0.6
  shared_preferences: ^2.5.3
  speech_to_text: ^6.6.2
  flutter_markdown_plus: ^1.0.7
  url_launcher: ^6.3.1
  flutter_stripe: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
